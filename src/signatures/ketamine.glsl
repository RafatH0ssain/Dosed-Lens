/* Ketamine — "the world becomes a flat painting at the end of a tunnel"
   sigWarp: vertical-divergence double vision + world recession (scale-down)
   sigColor: bilateral-style flattening, contrast crush, cold gray-blue desat
   sigTemporal: stochastic frame-hold stutter. Heavy: k-hole tunnel.
   Full module in M3. */

vec2 sigWarp(vec2 uv){ return uv; }
vec3 sigColor(vec3 col, vec2 uv){ return col; }
vec3 sigTemporal(vec3 col, vec2 uv){ return col; }
