/* DPH / deliriant — "not colorful — wrong. Spiders in corners, smoke" ⚠ gated
   No geometry, no rainbow. sigColor: desat, darken, sepia-green, blur waves.
   CPU particles: skitter clusters + smoke wisps (shared layer with meth).
   sigTemporal: shadow pareidolia in darkest regions, Heavy: figure events.
   Full module in M5. */

vec2 sigWarp(vec2 uv){ return uv; }
vec3 sigColor(vec3 col, vec2 uv){ return col; }
vec3 sigTemporal(vec3 col, vec2 uv){ return col; }
