{
  "name": "dosed-lens",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "samples": "node scripts/gen-samples.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^8.1.4"
  }
}
